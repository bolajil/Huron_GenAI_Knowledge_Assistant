# Huron GenAI Knowledge Assistant - Production Requirements
# For 20,000+ user enterprise deployment
# Install: pip install -r requirements-production.txt

# =============================================================================
# Database - PostgreSQL for production scale
# =============================================================================
psycopg2-binary>=2.9.9       # PostgreSQL adapter (sync)
asyncpg>=0.29.0              # PostgreSQL adapter (async for FastAPI)
sqlalchemy>=2.0.25           # ORM with connection pooling
alembic>=1.13.1              # Database migrations

# =============================================================================
# Caching & Message Queue - Redis for 20k scale
# =============================================================================
redis>=5.0.1                 # Redis client
celery>=5.3.6                # Async task queue
flower>=2.0.1                # Celery monitoring

# =============================================================================
# Web Framework - FastAPI for high concurrency
# =============================================================================
fastapi>=0.109.0
uvicorn[standard]>=0.27.0    # ASGI server with uvloop
gunicorn>=21.2.0             # Process manager
python-multipart>=0.0.6      # File uploads

# =============================================================================
# Authentication & Security
# =============================================================================
pyjwt>=2.8.0                 # JWT tokens
bcrypt>=4.1.2                # Password hashing
python-jose[cryptography]>=3.3.0  # JWT with crypto
passlib[bcrypt]>=1.7.4       # Password utilities
pyotp>=2.9.0                 # MFA TOTP
cryptography>=42.0.0         # Encryption

# =============================================================================
# LLM & Embeddings
# =============================================================================
openai>=1.12.0               # OpenAI API
langchain>=0.1.0             # LLM orchestration
langchain-openai>=0.0.5
langchain-community>=0.0.20
langgraph>=0.0.26            # LangGraph pipelines
langsmith>=0.0.87            # Tracing & evaluation
sentence-transformers>=2.3.1 # Local embeddings

# =============================================================================
# Vector Stores
# =============================================================================
pinecone-client>=3.0.0       # Pinecone (primary)
weaviate-client>=4.4.0       # Weaviate (fallback)
faiss-cpu>=1.7.4             # FAISS (local dev)
qdrant-client>=1.7.0         # Qdrant (optional)

# =============================================================================
# Document Processing
# =============================================================================
pypdf>=3.17.4                # PDF extraction
python-docx>=1.1.0           # Word documents
openpyxl>=3.1.2              # Excel files
unstructured>=0.12.0         # Advanced document parsing
tiktoken>=0.5.2              # Token counting

# =============================================================================
# Monitoring & Observability
# =============================================================================
prometheus-client>=0.19.0    # Metrics
opentelemetry-api>=1.22.0    # Distributed tracing
opentelemetry-sdk>=1.22.0
structlog>=24.1.0            # Structured logging
sentry-sdk>=1.39.0           # Error tracking

# =============================================================================
# Testing
# =============================================================================
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.26.0                # Async HTTP testing
ragas>=0.1.0                 # RAG evaluation

# =============================================================================
# Development Tools
# =============================================================================
pre-commit>=3.6.0            # Git hooks
ruff>=0.2.0                  # Linting & formatting
bandit>=1.7.7                # Security scanning
safety>=2.3.5                # Dependency vulnerability scanning
